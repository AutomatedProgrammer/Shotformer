class Enemy(Object):
    def init(self, name, width, height, x, y, image_path):
        super(Enemy, self).init(name, width, height, x, y, image_path)

    def update(self):
        pass

    
# There is probably a much better way of doing this